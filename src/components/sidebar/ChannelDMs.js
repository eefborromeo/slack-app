import React from "react";
import { ChannelDMsLayout } from "../styles";

export default function ChannelDMs({ children }) {
    return (
        <ChannelDMsLayout>
            { children }
        </ChannelDMsLayout>
    )
}